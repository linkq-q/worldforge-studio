// Untagged generated geometry represents ordinary painted, wood, stone, or clay
// surfaces. Keep it dielectric; metal/glass semantics must be explicit in data.
export const DEFAULT_PBR_ROUGHNESS = 0.65;
export const DEFAULT_PBR_METALNESS = 0;
